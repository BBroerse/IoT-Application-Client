export function currentMeasurements(state) {
  return state.currentMeasurements;
}

export function weeklyMeasurements(state) {
  return state.weeklyMeasurements;
}

export function currentWeatherApiData(state) {
  return state.currentWeatherApiData;
}

export function weeklyWeatherApiData(state) {
  return state.weeklyWeatherApiData;
}

export function weatherApiLastUpdated(state) {
  return state.weatherApiLastUpdated;
}
